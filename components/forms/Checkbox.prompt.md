Checkbox — blue fill + white check. For consent/agreement rows in the assessment form and portal.

```jsx
<Checkbox label="I agree to be contacted about my MCA debt" defaultChecked />
```

Controlled (`checked` + `onChange`) or uncontrolled (`defaultChecked`).
